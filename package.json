{
  "name": "hit-inspector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local dev tool + deployable static snapshot for inspecting Hit Engine data catalogs.",
  "scripts": {
    "dev": "vite",
    "snapshot": "node scripts/build-snapshot.js",
    "build": "node scripts/build-snapshot.js && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
